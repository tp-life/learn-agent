# Embedding 与向量检索

Embedding 模型把文本映射为固定维度的向量，语义相近的文本向量距离也近。bge-m3 输出 1024 维向量。

余弦相似度只比较向量方向、忽略长度，是文本检索的默认度量：cos(a,b) = a·b / (|a||b|)。语义编码在方向上而非模长上，所以长短不同但语义相近的文本得分依然高。

检索就是暴力最近邻：查询向量与库中每个向量算相似度，取 top-k。十万条以内纯内存暴力检索毫秒级完成，不需要 HNSW 这类近似索引。
