# Chunking 切分策略

文档不能整篇入库：向量会稀释主题，还可能超出 embedding 模型的输入上限。切分（chunking）是 RAG 的第一调参位。

常见策略：固定窗口加 overlap（每 N 字符切一刀，相邻块重叠 10-20%，防止上下文在边界被切断）；按结构切（按段落、标题边界切，文档类首选）；语义切（效果好但贵）。

块太大则噪声多、相似度被稀释；块太小则上下文不完整、答案断章取义。常见起点是 200-500 token 加 10-20% 重叠。
