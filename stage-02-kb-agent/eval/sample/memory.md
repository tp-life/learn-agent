# Agent 的长期记忆

长期记忆是跨会话持久化的事实与偏好（如用户偏好、历史结论），存外部存储；短期记忆是会话内的 messages 历史。

长期记忆与 RAG 技术栈相同（存储加检索），区别在于写入路径和召回时机：RAG 的知识是预先灌入的静态文档；记忆是会话中动态产生的，需要决定什么值得记、何时回忆。

回忆本质也是一次检索：把记住的东西当知识库查。召回时机有两种设计：每轮自动检索注入，或让模型主动调 recall 工具。
