# 项目规则（Agent 学习工作区）

本工作区是个人学习 + 求职导向的 AI agent 学习项目集合（Go + TypeScript，模型用 DeepSeek）。

## 代码注释规范（强制，所有代码必须遵守）

此项目的第一目的是**学习**，代码同时也是学习材料。因此注释标准高于一般生产项目：

1. **每个包（package）** 必须有包级注释：说明这个包解决什么问题、在整个 agent 链路中的位置。
2. **每个导出的类型、函数、接口** 必须有 doc 注释：不只写"是什么"，还要写"为什么这样设计"。
3. **关键概念**（如 ReAct 循环、tool_calls、对话历史即状态、embedding、向量检索）在首次出现的代码处，必须用注释解释其原理，必要时给出面试考察角度的提示。
4. **工具层（tools）** 每个工具必须注释清楚三件事：
   - 给模型看的部分（Name/Description/Schema）为什么这样写；
   - 执行部分的安全边界（输入校验、输出截断、防注入等）；
   - 可能的生产环境注意事项（超时、限流、成本）。
5. **反直觉或易踩坑的代码** 必须解释原因（例如：assistant 含 tool_calls 的消息为何必须原样放回历史）。
6. 注释用中文（学习材料），标识符、技术术语保持英文原文。
7. 注释密度宁可偏高也不删减关键解释，但不写废话（不重复代码字面意思）。

## 技术栈约定

- 模型：DeepSeek（`deepseek-chat` 为主，OpenAI 兼容 API）；embedding 用 bge-m3（硅基流动或本地 Ollama）。
- 语言：Go（后端/infra）、TypeScript（应用/全栈）。
- 不引入重型框架；先手写原理，再按需引入库，引入时需在注释中说明"这个库替代了我们手写的哪部分"。

## 渐进式开发约定

- 每个阶段先跑通最小可用版本，再按"练习"逐步加能力（流式、重试、评估等）。
- 改动后必须编译通过、测试通过（`go vet` / `go test`）。

## 练习任务标注约定（强制）

此项目是学习项目，AI 不得把所有代码写完——需要用户动手的部分必须显式标注。

1. **格式**：在代码中对应位置用 `// TODO(练习N): <标题>` 标注，块内必须包含：
   - 任务：要实现什么（具体到函数/方法签名建议）；
   - 提示：关键思路、涉及的库、容易踩的坑（提示不给出完整实现）；
   - 验收：怎么算完成（可运行的验证方式）。
2. **AI 只写骨架和基础代码**；凡是阶段文档"下一步/练习"清单里的条目，必须在代码中有对应 TODO 标注，且不得由 AI 提前实现到项目代码里。
3. **无练习的模块也要显式说明**：在包注释或文件头部加一行 `// 练习：本模块无需用户完成的部分`。
4. 用户完成练习后，由用户删除 TODO 标注并更新阶段文档中对应练习的状态。

## 练习参考答案约定（强制）

每个 TODO 练习在出题的同时，必须把**参考答案**单独存放，供用户完成后对照映照：

1. **存放位置**：`docs/solutions/stage-XX/exercise-N-<名称>.md`，与阶段编号、练习编号一一对应。
2. **文档结构**（三节固定）：
   - 参考实现：完整可编译的代码（这是答案，不进项目代码树）；
   - 关键设计点：为什么这么写，标出实现中的易错处；
   - 对照清单：用户完成后逐条自评的检查项（不以"和答案一字不差"为标准，以清单条目覆盖为准）。
3. **互链**：代码 TODO 块里标注 `参考答案：docs/solutions/...（完成后再看）`；答案文档头部回链到 TODO 所在文件。
4. 用户完成练习并自评后，可以在对照中提出差异点讨论；差异只要是合理设计选择即可，不强制与答案一致。
5. **答案正确性（强制）**：参考答案必须保证正确、可用、与项目当前代码兼容——
   - 答案中的代码必须能通过 `go build` / `go vet`（必要时实际粘贴进项目验证后再写入文档）；
   - 必须考虑与项目已有机制的交互：答案不得为了简化而绕开核心机制（反面案例：SSE 流式答案不考虑 tool_calls，导致接入时只能绕过 ReAct 循环）；
   - 项目代码演进超出答案范围时，必须同步更新答案文档，保持"教程—答案—代码"三者一致；
   - 答案若分"基础版/完整版"，必须明确标注基础版的局限性及为何不推荐直接用于项目。

## 文档沉淀约定（强制）

目标：任何时候发起新对话，仅凭仓库文档就能恢复"目标 + 进度 + 下一步"。

1. **总览**：`docs/ROADMAP.md` 维护总目标、分阶段计划表（概念/工具/项目）、知识拓扑图（Mermaid）、进度速览表。每次完成里程碑必须更新进度表。
2. **阶段沉淀**：每个阶段对应 `docs/stages/stage-XX-<名>.md`，结构固定为七节：
   - 这个阶段在学什么
   - 核心概念（必须能脱稿讲出来的程度）
   - **知识梳理（复习资料）**：本阶段知识的系统化总结，供留档与后续复习。要求：自问自答式考点清单（覆盖面试高频问题）、易混淆概念对比表、关键流程图/时序图；写完要达到"只看这一节就能通过本阶段面试提问"的标准
   - 注意事项（踩过的坑、易错点，逐条编号累积）
   - 已完成
   - 下一步（练习/任务清单，带状态）
   - 阶段验收标准（checklist）
3. **进入新阶段时**：先更新 ROADMAP 状态，再创建新阶段文档（含前置阅读链接），旧阶段文档标记验收结果。
4. **专题文档**（如 `docs/embedding-vectordb-guide.md`）在 ROADMAP 的"文档地图"登记。
5. 文档用中文；状态图例统一：✅ 完成 / 🔄 进行中 / 📖 学习中 / ⬜ 未开始。
